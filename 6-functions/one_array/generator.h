#include <math.h>

int* array_generator(int* real_size, int k, int m, int f){
    int *arr = malloc((int)sizeof* real_size);
    int summ = 0;
    for (int i =0; i< *real_size;i++){
        return 0;
    }

    return 0;
}
